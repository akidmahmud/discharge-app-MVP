# Migrations

Place future one-off migration or repair scripts here instead of the project
root.

Rules

- Keep this directory blocked from direct web access via `.htaccess`.
- Run migrations through a protected admin/module flow, not by exposing
  browser-runnable PHP files at the site root.
